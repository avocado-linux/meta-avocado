# Load the zram kernel module at boot so /dev/zram0 appears before
# systemd-zram-setup@zram0.service tries to set up the swap device.
# The systemd-zram-generator binary configures the device but does not
# modprobe the module itself.
zram
