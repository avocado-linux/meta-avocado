#!/usr/bin/env bash
# Thin wrapper: the work lives in avocado-build-qcom, shared by every QCS6490
# board in this layer. The SDK installs one of these per machine because
# avocado-cli invokes the hook by target name.
exec "$(dirname "$(readlink -f "$0")")/avocado-build-qcom" rb3gen2 "$@"
