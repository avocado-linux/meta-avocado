# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-avocado-stm"
BBFILE_PATTERN_meta-avocado-stm = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-avocado-stm = "9"

LAYERDEPENDS_meta-avocado-stm = "core"
LAYERSERIES_COMPAT_meta-avocado-stm = "wrynose"
