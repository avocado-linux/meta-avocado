# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-avocado-shared"
BBFILE_PATTERN_meta-avocado-shared = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-avocado-shared = "9"

LAYERDEPENDS_meta-avocado-shared = "core"
LAYERSERIES_COMPAT_meta-avocado-shared = "scarthgap"
