# U-Boot env source: nowhere (RAM-only).
# The bootloader chain is immutable post-Phase-1; the OS image carries its own
# extlinux.conf on the boot partition, so no persistent env is needed.
# CONFIG_ENV_IS_IN_FLASH is not set
# CONFIG_ENV_IS_IN_SPI_FLASH is not set
# CONFIG_ENV_IS_IN_MMC is not set
CONFIG_ENV_IS_NOWHERE=y

CONFIG_USE_BOOTCOMMAND=y
CONFIG_BOOTCOMMAND="setenv boot_targets mmc0 mmc1; run distro_bootcmd; fastboot usb 0"

CONFIG_DISTRO_DEFAULTS=y
CONFIG_CMD_PXE=y
CONFIG_CMD_SYSBOOT=y
CONFIG_BOOTSTD_FULL=y
