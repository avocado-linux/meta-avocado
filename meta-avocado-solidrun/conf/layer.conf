# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-avocado-solidrun"
BBFILE_PATTERN_meta-avocado-solidrun = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-avocado-solidrun = "9"

LAYERDEPENDS_meta-avocado-solidrun = "core"
LAYERSERIES_COMPAT_meta-avocado-solidrun = "scarthgap"
