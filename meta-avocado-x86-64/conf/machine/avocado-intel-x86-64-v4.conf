#@TYPE: Machine
#@NAME: Intel x86-64-v4 machine
#@DESCRIPTION: x86-64-v4 (AVX-512) for CPUs with full AVX-512 support
#
# Target processors and codenames:
#   Intel: Panther Lake (Cougar Cove + Darkmont, AVX10.1-512)
#   Intel Xeon: Skylake-SP, Cascade Lake, Ice Lake-SP, Sapphire Rapids,
#               Emerald Rapids, Granite Rapids, Clearwater Forest
#   AMD: Zen 4 (Ryzen 7000, EPYC Genoa), Zen 5 (Ryzen 9000, EPYC Turin)
#
# NOTE: Does NOT work on Intel hybrid client CPUs before Panther Lake
#       (Alder Lake through Arrow Lake) because their E-cores lack AVX-512.
#
# Key instruction sets: AVX-512 (F, VL, BW, CD, DQ)

STONE_PROVISIONING ?= "img usb peridio"
MACHINEOVERRIDES =. "intel-x86-64-v4:"

require conf/machine/include/avocado.inc
require conf/machine/include/avocado-x86-64.inc

DEFAULTTUNE ?= "x86-64-v4"
require conf/machine/include/x86/tune-x86-64-v4.inc

PREFERRED_PROVIDER_virtual/kernel = "linux-yocto"

MACHINE_FEATURES += "x86 pci efi"

# QEMU usermode cannot emulate AVX-512 instructions via TCG, so disable
# the exe_wrapper that Meson uses for cross-compilation sanity checks.
MACHINE_FEATURES_BACKFILL_CONSIDERED += "qemu-usermode"

DISTRO_FEATURES += "tpm2"
PACKAGECONFIG:append:pn-systemd = " tpm2"
