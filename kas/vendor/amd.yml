header:
  version: 16

local_conf_header:
  vendor-amd: |
    PREFERRED_PROVIDER_virtual/kernel = "linux-yocto"
    DISTRO_FEATURES_EXTRA:append = " opengl wayland efi seccomp virtualization"

    PKG_EXTRA_INSTALL:append = " packagegroup-avocado-amd-x86-64-extra"
