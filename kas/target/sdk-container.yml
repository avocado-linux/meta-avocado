header:
  version: 16

repos:
  meta-avocado:
    path: meta-avocado
    layers:
      meta-avocado-sdk:

local_conf_header:
  target-container: |
    DISTRO_FEATURES_EXTRA:append = "virtualization"

target:
  - avocado-image-container
